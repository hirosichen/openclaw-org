---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={['glass rounded-xl p-6 transition-all duration-200 hover:border-accent/30', className]}>
  <slot />
</div>
